# Google Maps Configuration
# Required: Your Google Maps API key (must have Maps JavaScript API and Directions API enabled)
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Pinned Location Coordinates
# The destination marker location (latitude and longitude)
VITE_PIN_LAT=40.7128
VITE_PIN_LONG=-74.0060

# Note: In Vite projects, environment variables must be prefixed with VITE_ 
# to be exposed to the client-side code.
